
clear
clc

syms t b B Vm(t) Im(t) um(t) vm(t) us(t) vs(t)

disp(2+2)

%% Case1: Define the matrixes

A = [0 1/sqrt(2*b); sqrt(2*b)/(1+B) 0]
B = [(b*Vm(t))/sqrt(2*b); sqrt(2*b)/(1+B)*vm]
X = [um;Im]

%% Solve the system

X = inv(A)*B

%% ase 2
C = []
D = []
Y = 

%% Solve the 2nd system